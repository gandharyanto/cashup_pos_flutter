name: cashup_pos
description: "Cashup POS SDK — full end-to-end Point of Sale UI and calculation engine, embeddable into any Flutter application."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.2
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  dio: ^5.7.0
  intl: ^0.20.2
  collection: ^1.19.0
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
